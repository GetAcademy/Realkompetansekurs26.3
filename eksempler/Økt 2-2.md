# Økt 2-2 — Variabler, aritmetiske operatorer, if-setninger og ternary operator
**Dato:** torsdag 20. august 2026
Opptak: Kommer etter undervisningen
---

## Mål for økten
- Deklarere, tilordne og lese variabler med `let` og `const`
- Bruke aritmetiske operatorer
- Skrive enkle if-setninger med sammenligningsoperatorer
- Skrive korte betingelser med ternary operator (`? :`)

---

## Del 1 — Variabler

### Hva er en variabel?
En variabel er en **navngitt huskelapp** i programmet.

### Demo 1 — Deklarasjon og tilordning

Skriv i konsollen, steg for steg:

```js
// Deklarasjon: lage variabelen
let alder = 25;

// Lese: hva er verdien?
console.log(alder);       // 25

// Tilordning: endre verdien
alder = 26;
console.log(alder);       // 26
```

Poeng:
- `let` brukes når verdien kan endre seg
- `const` brukes når verdien ikke skal endre seg
- Variabelnavnet velger vi selv — bruk beskrivende navn

```js
const navn = "Kari";
navn = "Ole";  // FEIL — const kan ikke endres
```

### Demo 2 — Variabler på siden

```html
<p id="output"></p>

<script>
    let alder = 25;
    let navn = "Kari";

    document.getElementById("output").innerHTML =
        navn + " er " + alder + " år gammel.";
</script>
```

---

## Del 2 — Aritmetiske operatorer

```js
let a = 10;
let b = 3;

console.log(a + b);   // 13  — pluss
console.log(a - b);   // 7   — minus
console.log(a * b);   // 30  — gange
console.log(a / b);   // 3.33... — dele
```

### Demo 3 — Enkel kalkulator

```html
<input id="tall1" type="number" placeholder="Tall 1">
<input id="tall2" type="number" placeholder="Tall 2">
<button onclick="regnUt()">Regn ut</button>
<p id="svar"></p>

<script>
    function regnUt() {
        let tall1 = Number(document.getElementById("tall1").value);
        let tall2 = Number(document.getElementById("tall2").value);
        let resultat = tall1 + tall2;
        document.getElementById("svar").innerHTML = "Svar: " + resultat;
    }
</script>
```

Poeng:
- `Number(...)` konverterer tekst fra inputfeltet til et tall
- Uten `Number()` vil `"3" + "5"` bli `"35"` (tekstsammenslåing) ikke `8`

---

## Del 3 — If-setninger 

### Hva er en if-setning?
Kode som bare kjøres *hvis* noe er sant.

### Demo 4 — Enkel if/else

```js
let alder = 20;

if (alder >= 18) {
    console.log("Du er voksen.");
} else {
    console.log("Du er ikke voksen ennå.");
}
```

### Sammenligningsoperatorer

| Operator | Betyr | Eksempel |
|---|---|---|
| `===` | er lik | `alder === 18` |
| `!==` | er ikke lik | `navn !== "Kari"` |
| `>` | større enn | `poeng > 50` |
| `<` | mindre enn | `poeng < 50` |
| `>=` | større enn eller lik | `alder >= 18` |
| `<=` | mindre enn eller lik | `alder <= 65` |

### Demo 5 — If på siden

```html
<input id="alderInput" type="number" placeholder="Skriv alder">
<button onclick="sjekkAlder()">Sjekk</button>
<p id="svar"></p>

<script>
    function sjekkAlder() {
        let alder = Number(document.getElementById("alderInput").value);

        if (alder >= 18) {
            document.getElementById("svar").innerHTML = "Du er voksen!";
        } else {
            document.getElementById("svar").innerHTML = "Du er ikke voksen ennå.";
        }
    }
</script>
```

### Demo 6 — if / else if / else

```js
function karakter(poeng) {
    if (poeng >= 90) {
        return "A";
    } else if (poeng >= 70) {
        return "B";
    } else if (poeng >= 50) {
        return "C";
    } else {
        return "Stryk";
    }
}
```

---

## Del 4 — Ternary operator

### Hva er ternary operator?
En kortform for if/else — brukes når begge grenene bare skal gi *én verdi*.

### Demo 7 — If/else vs. ternary

```js
let alder = 20;
let melding;

if (alder >= 18) {
    melding = "Voksen";
} else {
    melding = "Ikke voksen";
}

// Samme resultat med ternary:
let melding2 = alder >= 18 ? "Voksen" : "Ikke voksen";
```

Poeng:
- Oppskrift: `betingelse ? verdiHvisSann : verdiHvisUsann`
- Ternary er et **uttrykk** — det gir en verdi tilbake, akkurat som `+` eller `*`
- Passer best når begge utfallene bare setter én verdi — ikke når det skal skje flere ting

### Demo 8 — Ternary rett inn i `innerHTML`

```html
<input id="alderInput" type="number" placeholder="Skriv alder">
<button onclick="sjekkAlder()">Sjekk</button>
<p id="svar"></p>

<script>
    function sjekkAlder() {
        let alder = Number(document.getElementById("alderInput").value);
        document.getElementById("svar").innerHTML =
            alder >= 18 ? "Du er voksen!" : "Du er ikke voksen ennå.";
    }
</script>
```

Poeng:
- Samme resultat som Demo 5 — men uten `if`, `else` og en egen `melding`-variabel
- Nyttig når vi bare skal *velge mellom to verdier* rett inn i `innerHTML`
- Unngå å nøste ternary i ternary (`a ? b : c ? d : e`) — da blir det raskt uleselig. Bruk `if/else if/else` når det er mer enn to utfall.

---

## Læringsaktivitet — Aldersjekker eller kalkulator

**Alternativ A:** Lag en aldersjekker. Brukeren skriver inn alder, trykker på en knapp, og siden viser en melding basert på alderen (f.eks. under 18, 18–67, over 67). Bruk ternary for selve meldingen.

**Alternativ B:** Utvid kalkulatoren fra demoen med fire knapper: pluss, minus, gange og dele.

Last opp til GitHub.

---

## Vanlige feil

| Feil | Årsak | Løsning |
|---|---|---|
| `"3" + "5"` gir `"35"` | Input er tekst, ikke tall | Pakk inn i `Number(...)` |
| `=` i stedet for `===` | Tilordning vs. sammenligning | `=` setter verdi, `===` sjekker verdi |
| `const` gir feil ved endring | `const` kan ikke tilordnes på nytt | Bruk `let` hvis verdien skal endre seg |
| Glemt `:` i ternary | Syntaksfeil | Husk begge delene: `betingelse ? sant : usant` |

---

## Tips 
- Konsolltesting er nøkkelen — skriv `let x = 5; x + 3` og se resultatet
- `Number()`-konverteringen er litt magisk —  "input alltid er tekst"
- Fokuser på `if/else/else if`, ternary er bonus
